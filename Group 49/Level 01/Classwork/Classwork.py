name = "Nikoloz"
surname = "Kapanadze"

#name არის ცვლადი
# = - არის ცვლადის მნიშვნელობის მიმნიჭებელი სიმბოლო
#"Nikoloz" არის ცვლადისთვის მნივშვნელობა


print(name)

#პრინტ ფუნქციას გადაეცემა ეკრანზე გამოსატანი ობიექტი

print(surname)

name = "Nikoloz" #ეს არის str(string) ტიპის ცვლადი
age = 14 #ეს არის int (integer) მთელი რიცხვი
height = 173.5 #ეს არის float ტიპის ცვლადი (ათწილადი)
#booLean (bool) ტიპის ცვლადი
knows_programming = True 
is_ugly = False #snakecase (წერის სტილი)

print(name + " " + surname)
print(name + " " + str(age))